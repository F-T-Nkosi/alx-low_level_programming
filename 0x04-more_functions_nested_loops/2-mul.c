#include<stdio.h>
#include "main.h"

/**
 * mul - multiplies two inputs
 * @a: is the first num input
 * @b: is the second num input
 * Return: result of a*b
 */
int mul(int a, int b)
{
	return (a * b);
}
