#include<unistd.h>

/**
 * main - writes the character c to stsout
 * @c: The charcater to print
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _putchar(char c);
